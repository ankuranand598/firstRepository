//it is a program of compound intrest.
var a ; //final amount.
var p = 1000; // principal amount
var t = 6; // time 
var r = 4; // rate
var n = 1; // no of time in a year the amount is applied
a = p * (1 + r/ n) ** (n * t) ; //formula of simple int.
console.log(a);